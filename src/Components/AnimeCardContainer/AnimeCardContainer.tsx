import React from 'react';

interface AnimeCardContainerProps {
    data: object
}

const AnimeCardContainer: React.FC<AnimeCardContainerProps> = ({ data }) => {
    return (
        <div>

        </div>
    );
};

export default AnimeCardContainer;
